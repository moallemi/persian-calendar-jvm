plugins {
    id "com.jfrog.bintray" version "1.8.4"
    id "maven-publish"
    id "signing"
}

subprojects {
    repositories {
        google()
        jcenter()
    }
}

// apply from: 'gradle/publish.gradle'